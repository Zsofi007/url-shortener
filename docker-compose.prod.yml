services:
  postgres:
    image: postgres:15-alpine
    container_name: url_shortener_postgres_prodlike
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-urlshortener}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: url_shortener_backend_prodlike
    environment:
      DEBUG: "False"
      BASE_URL: ${BASE_URL:-http://localhost:8000}
      BASE_URL_FRONTEND: ${BASE_URL_FRONTEND:-http://localhost:3000}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}
      # Use a local DB by default inside Docker. If you want a remote DB, set DATABASE_URL_DOCKER.
      DATABASE_URL: ${DATABASE_URL_DOCKER:-postgresql://postgres:postgres@postgres:5432/urlshortener}
      SUPABASE_URL: ${SUPABASE_URL:-}
      SUPABASE_PUBLISHABLE_KEY: ${SUPABASE_PUBLISHABLE_KEY:-}
      SUPABASE_SECRET_KEY: ${SUPABASE_SECRET_KEY:-}
      JWT_SECRET: ${JWT_SECRET:-prodlike-secret}
      JWT_EXPIRY: ${JWT_EXPIRY:-3600}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./url_frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    container_name: url_shortener_frontend_prodlike
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:

